const TheError = ({ children }) => {
    return (
        <>
            <p className="w-full py-8 text-center text-sm font-bold text-rose-600">{children}</p>
        </>
    );
};

export default TheError;
